{
  // Static-assets-only Worker: no `main` script, Cloudflare just serves ./dist.
  // `name` must match the Workers project name in the dashboard.
  "name": "tablo",
  "compatibility_date": "2026-08-11",
  "assets": {
    "directory": "./dist"
  }
}
