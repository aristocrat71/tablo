{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities shared by the avatar, panel, and dashboard windows",
  "windows": ["avatar", "panel", "dashboard"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "notification:default"
  ]
}
